# Reload config
# unbind r
# bind r source-file ~/.tmux.conf \; display-message "Config reloaded..."
#
# # Prefix and mouse
# set -g prefix C-s
# set -g mouse on
#
# # Smart pane switching with Vim awareness
# bind -n C-h select-pane -L
# bind -n C-j select-pane -D
# bind -n C-k select-pane -U
# bind -n C-l select-pane -R
#
# # Terminal settings
# set -g default-terminal "tmux-256color"
# set -gq allow-passthrough on
# set -g visual-activity off
# set-option -g automatic-rename on
# set -wg automatic-rename on
# set -g automatic-rename-format "Window"
# set -g set-titles on
# set -g set-titles-string '#W'
#
# # Plugin list
# set -g @plugin 'tmux-plugins/tpm'
# set -g @plugin 'tmux-plugins/tmux-sensible'
# set -g @plugin 'tmux-plugins/tmux-cpu'
# set -g @plugin 'tmux-plugins/tmux-battery'
# set -g @plugin 'christoomey/vim-tmux-navigator'
# set -g @plugin 'rose-pine/tmux'
#
# ##### 🌹 Rose Pine Theme Config #####
# set -g @rose_pine_variant 'main'                 # Options: main, moon, dawn
# set -g @rose_pine_user 'on'                      # Enable username
# set -g @rose_pine_username_icon '󰠬 '             # Customize user icon
# set -g @rose_pine_date_time '%m/%d/%Y'           # Custom date format
# set -g @rose_pine_left_separator '  '           # Left separator
# set -g @rose_pine_right_separator '  '          # Right separator
# set -g @rose_pine_window_status_separator ' 󰳭 '  # Separator between windows
# set -g @rose_pine_host 'on'                      # Show host
# set -g @rose_pine_bar_bg_disable 'on'            # Transparent bar for terminal
# set -g @rose_pine_bar_bg_disabled_color_option '0'
# set -g @rose_pine_only_windows 'off'             # Set to 'on' for minimalist status
# set -g @rose_pine_disable_active_window_menu 'on'
#
# # Set status line position
# set-option -g status-position top
#
# # Modules
# run '~/.config/tmux/plugins/tmux-plugins/tmux-cpu/cpu.tmux'
# run '~/.config/tmux/plugins/tmux-plugins/tmux-battery/battery.tmux'
#
# # Initialize TMUX plugin manager (must be last)
# run '~/.tmux/plugins/tpm/tpm'


unbind r
bind r source-file ~/.tmux.conf \; display-message "Config reloaded..."

set -g prefix C-s
set -g mouse on

# Smart pane switching with awareness of Vim splits
bind -n C-h select-pane -L
bind -n C-j select-pane -D
bind -n C-k select-pane -U
bind -n C-l select-pane -R

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'catpuccin/tmux'
set -g @plugin 'tmux-plugins/tmux-cpu'
set -g @plugin 'tmux-plugins/tmux-battery'
set -g @plugin "christoomey/vim-tmux-navigator"

set-option -g status-position top

# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'github_username/plugin_name#branch'
# set -g @plugin 'git@github.com:user/plugin'
# set -g @plugin 'git@bitbucket.com:user/plugin'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'

set -g default-terminal "tmux-256color"

# Configure the catppuccin plugin
set -g @catppuccin_flavor "mocha"
set -g @catppuccin_window_status_style "rounded"

# Load catppuccin
run ~/.config/tmux/plugins/catppuccin/tmux/catppuccin.tmux
# For TPM, instead use `run ~/.tmux/plugins/tmux/catppuccin.tmux`

# Make the status line pretty and add some modules
set -g status-right-length 100
set -g status-left-length 100
set -g status-left ""
set -g status-right "#{E:@catppuccin_status_application}"
set -agF status-right "#{E:@catppuccin_status_cpu}"
set -ag status-right "#{E:@catppuccin_status_session}"
set -ag status-right "#{E:@catppuccin_status_uptime}"
set -agF status-right "#{E:@catppuccin_status_battery}"

run ~/.config/tmux/plugins/tmux-plugins/tmux-cpu/cpu.tmux
run ~/.config/tmux/plugins/tmux-plugins/tmux-battery/battery.tmux

set -gq allow-passthrough on
set -g visual-activity off

set-option -g automatic-rename on
set -wg automatic-rename on
set -g automatic-rename-format "Window"
set -g set-titles on
set -g set-titles-string '#W'  # Shows the window name (like "vim" or "top")
set -g @catppuccin_window_default_text " #W"
set -g @catppuccin_window_current_text " #W"
set -g @catppuccin_window_active_text " #W"
set -g @catppuccin_window_current_text " #{window_name}"
set -g @catppuccin_window_text " #{window_name}"
